<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".director_programa.Reportes">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:padding="22dp"
        android:layout_height="wrap_content">


        <Button
            android:layout_width="match_parent"
            android:text="Reporte Mortalidad"
            android:background="@drawable/background_boton"
            android:textColor="#fff"
            android:layout_marginTop="135dp"
            android:id="@+id/btnMortalidad"
            android:layout_height="wrap_content"/>

        <Button
            android:layout_width="match_parent"
            android:background="@drawable/background_boton"
            android:textColor="#fff"
            android:layout_marginTop="12dp"
            android:text="Reporte Inasistencias"
            android:id="@+id/btnInacistencias"
            android:layout_height="wrap_content"/>

    </LinearLayout>

</FrameLayout>